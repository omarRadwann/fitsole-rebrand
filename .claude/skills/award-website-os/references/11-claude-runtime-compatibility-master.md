# 11 — Claude Code Runtime Compatibility Master

This pack is Claude Code-only.

Use repository skills under `.claude/skills/`, custom agents under `.claude/agents/`, hooks configured in `.claude/settings.json` and scripts under `.claude/hooks/`, and evidence docs under `docs/`.

Do not depend on hooks alone. Run the make commands manually when hook execution is not confirmed.
