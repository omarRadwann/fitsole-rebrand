# Real Masters Website Agent OS

This is the corrected, deeper version of the Moon-Level Agents Pack.

## Honest Status

This pack does not magically contain "all possible data" — no file can.

What it does contain:
- real specialist agent definitions
- deep knowledge bases behind the agents
- operating protocols
- research workflow
- concept/art direction workflow
- copy/conversion workflow
- asset sourcing/generation rules
- Blender production workflow
- Three.js/R3F/WebGL/shader workflow
- GSAP/motion workflow
- performance and accessibility standards
- screenshot critique rules
- release QA rules
- Codex/Claude compatibility loaders

## Install

Copy these into your repo:

- `AGENTS.md`
- `.agents/skills/award-website-os/`
- `.codex/agents/`
- `.codex/hooks.json` if useful

For Claude Code also copy:
- `CLAUDE.md`
- `.claude/agents/`
- `.claude/settings.json`

## Use

Prompt:

```txt
Use the Award Website OS Real Masters workflow.
Business type: luxury dental clinic in Cairo.
Build the full website from zero to ship-ready.
Infer the brief, research the category, create concepts, generate/source assets if needed, build, inspect screenshots, and run QA gates.
```

## Tool Requirements

For full power, connect:
- web search
- browser/screenshot tools
- file system
- shell
- image generation
- Blender
- Playwright
- Lighthouse
- axe
- Figma/MCP if needed
- deployment tool if wanted

## Safety

No paid tools, subscriptions, purchases, publishing, or external copyrighted assets without explicit user approval.


## Final Smart Pass Added

This final version adds the missing senior "tips and tricks" layer:

- anti-template heuristics
- Awwwards-style taste rubric
- AI-genericity removal protocol
- Codex prompt/context engineering
- visual composition cheats
- motion microinteraction recipes
- WebGL/3D performance tricks
- image generation prompt library
- hooks/evals/auto-repair loops
- reference mining system
- production debugging playbook
- launch/post-launch growth checklist
- localization/cultural edge playbook
- Blender procedural recipes
- Three.js/R3F code recipes
- no-template checklist
- agent collaboration patterns
- smart dependency decisions

Use the final smart check:

```bash
bash .agents/skills/award-website-os/scripts/final-smart-check.sh
```


## V4 Zero-Gap Patch

This version fixes the final structural gaps:

- Codex agents are now real `.toml` custom agent files.
- Claude agents remain `.md` files with YAML frontmatter.
- Skill references now exist in `references/` as expected by Codex.
- Templates are also mirrored to `assets/templates/`.
- Added `agents/openai.yaml` for Codex skill metadata.
- Added `.codex/config.toml`.
- Rebuilt `.codex/hooks.json` and `.claude/settings.json` with real hook shapes.
- Added readiness, evidence creation, preflight, and final smart check scripts.
- Added evidence artifact requirements.
- Added zero-gap governance and truthfulness labels.

Run:

```bash
bash .agents/skills/award-website-os/scripts/tool-readiness-check.sh
bash .agents/skills/award-website-os/scripts/create-evidence-files.sh
bash .agents/skills/award-website-os/scripts/final-smart-check.sh
```

## V6 Patient Audit Corrections

This package now includes both skill locations:

- Codex: `.agents/skills/award-website-os/`
- Claude Code: `.claude/skills/award-website-os/`

Claude subagents now preload the `award-website-os` skill, and hook commands have safer root fallbacks. Windows PowerShell companion scripts are included for evidence creation and final checks.



## V7 Line-by-Line Audit Note

This package includes `MASTER_DOCUMENT.md`, `AUDIT_LINE_BY_LINE_REPORT.md`, and `AUDIT_LINE_BY_LINE_MAP.csv`. Use this package as the single source of truth. Run `python ops/validate_pack.py` after copying it into a repo.
